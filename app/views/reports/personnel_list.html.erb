<section class="content">
  <div class="row">
    <div class="col-md-12">
      <!-- Buttons with Icons -->
      <div class="card">
        <div class="card-body row">
          <div>
            <button onclick="downloadPersonnelListCSV()" type="button" class="btn btn-primary">
              <i class="fa fa-file-csv"></i> Download CSV
            </button>
            <button onclick="downloadPersonnelListWorkBook()" type="button" class="btn btn-primary">
              <i class="fa fa-file-excel"></i> Download Excel
            </button>
            <button onclick="downloadPersonnelListPDF()" type="button" class="btn btn-primary">
              <i class="fa fa-file-pdf"></i> Download PDF
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">

          <div class="card-body">
            <table id="example1" class="table-striped table-condensed" style="width: 100%;">
              <thead>
              <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Personnel Number</th>
                <th>Email</th>
                <th>Phone #</th>
                <th>Last Update</th>
              </tr>
              </thead>
              <tbody>
              <% @people.each do |person| %>
                <tr>
                  <td><%= person.first_name %></td>
                  <td><%= person.last_name %></td>
                  <td><%= person.barcode %></td>
                  <td><%= person.email %></td>
                  <td><%= person.phone %></td>
                  <td><%= person.updated_at.strftime("%d.%m.%Y") %></td>
                </tr>
              <% end %>
              </tbody>
              <tfoot>
              <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Personnel Number</th>
                <th>Email</th>
                <th>Phone #</th>
                <th>Last Update</th>
              </tr>
              </tfoot>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
        <!-- /.card -->
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>

<script type="text/javascript">
    $(document).ready(function () {
        jQuery('.personnel-list').addClass('active');
        jQuery('.other-reports').addClass('active');
        jQuery('.other-reports-menu').addClass('menu-open');

        $("#example1").DataTable({
            "responsive": true,
            "lengthChange": false,
            "autoWidth": false,
            "buttons": ["colvis"]
        }).buttons().container().appendTo('#example1_wrapper .col-md-6:eq(0)');

        jQuery(".dt-buttons").removeClass("btn-group")
        jQuery(".dt-buttons > .btn-secondary").removeClass("btn-secondary").addClass("btn-primary")
    })

    function downloadPersonnelListCSV(){
        window.location = "/personnel_list_csv"
    }
    function downloadPersonnelListWorkBook(){
        window.location = "/personnel_list_work_book"
    }
    function downloadPersonnelListPDF(){
        window.location = "/download_personnel_list_pdf"
    }

</script>